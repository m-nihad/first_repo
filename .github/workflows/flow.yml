on : push
jobs :
    first-job:
        runs-on : ubuntu-latest
        steps :
          - uses: actions/setup-node@v2
            with:
              node-version: 14.20.1
          - uses: actions/checkout@v3
          - runs : node --version
          - runs : npm --version
